name: Pre-Release
on:
  pull_request:
  workflow_dispatch:
jobs:
  pre-release:
    uses: defi-wonderland/aztec-ci-actions/.github/workflows/pre-release.yml@feat/gh-prerelease
    with:
      package-dir: src/ts
    secrets: inherit
    permissions:
      contents: write