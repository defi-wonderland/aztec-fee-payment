name: Pre-Release

on:
  workflow_dispatch:

jobs:
  pre-release:
    uses: defi-wonderland/aztec-ci-actions/.github/workflows/pre-release.yml@v0
    with:
      package-dir: export/@defi-wonderland/aztec-fee-payment
    secrets: inherit
    permissions:
      contents: write
